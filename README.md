# lab

One-off static projects, FTP-style. Each folder = a path on lab.philipbaker.us.

Ship: drop a folder with an index.html, then deploy (ask Claude, or `npx vercel --prod`).

Some folders are unlisted client links (on neither the index nor the sitemap) — CLAUDE.md
says which, and how each ships.
